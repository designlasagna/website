{
  "name": "designlasagna-website",
  "version": "0.0.0",
  "private": true,
  "description": "Design Lasagna public site — Eleventy static site.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "check": "eleventy --quiet && test -f dist/index.html && test -f dist/404.html && test -f dist/CNAME && test -f dist/assets/styles.css && test -f dist/assets/logotype.svg",
    "deploy-preview": "npx serve dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@designlasagna/schemas": "^0.3.4"
  }
}
